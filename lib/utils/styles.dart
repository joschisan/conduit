import 'package:flutter/material.dart';

const smallStyle = TextStyle(fontSize: 14);
const mediumStyle = TextStyle(fontSize: 18);
const largeStyle = TextStyle(fontSize: 26);
const heroStyle = TextStyle(fontSize: 42);

const smallIconSize = 28.0;
const mediumIconSize = 32.0;
const largeIconSize = 48.0;
const heroIconSize = 64.0;

const listTilePadding = EdgeInsets.symmetric(horizontal: 16, vertical: 8);
const borderRadiusLarge = BorderRadius.all(Radius.circular(12));
